import 'package:flutter/material.dart';

class WishEditWidget extends StatelessWidget {
  const WishEditWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
